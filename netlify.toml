[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA Routing — semua path diarahkan ke index.html
# agar React Router bisa menangani navigasi
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
